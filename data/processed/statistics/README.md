# Frozen statistics

The reported result JSON and source-data tables are the public sufficient-statistic layer. Exact raw-to-statistics replay requires reviewer-only evidence. No-model reproduction checks the reported quantities and regenerates all figures without changing endpoints, thresholds, multiplicity or result labels.
