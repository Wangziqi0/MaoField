# Renderers

GLM exact renderer code is included under `src/glm_runtime/`. Phase 23 renderer sources are referenced by content hashes in the frozen lineage but require import from the immutable reviewer archive before public release. Do not substitute reconstructed or post-result renderers.
