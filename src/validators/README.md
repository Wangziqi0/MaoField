# Validators

GLM validation and mutation sources are included under `src/glm_runtime/`. Phase 23 final validators and their receipts require import from the reviewer-only execution archive. Validation receipts are not scientific effects and must remain distinct from endpoint results.
