# Scorers

GLM scorer A/B implementations are included under `src/glm_runtime/`. Phase 23 scorer sources must be copied byte-for-byte from the final execution archive. Reported processed results and figures can be reproduced without those model-facing files; full trajectory-to-score replay cannot.
