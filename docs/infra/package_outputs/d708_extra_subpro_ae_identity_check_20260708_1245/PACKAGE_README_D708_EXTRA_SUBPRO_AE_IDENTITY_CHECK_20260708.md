# D708 Extra SubPro A/E Identity Check Package README

## Package Purpose

This package is for two extra GPT-5.5 Pro review sessions after report40
accepted the D707/D708 internal v0 note only as:

```text
ACCEPT_INTERNAL_V0_AS_LOCAL_NOTE_ONLY
KEEP_INTERNAL_ONLY
```

The goal is not to write a paper. The goal is to double-check:

- finite mathematical correctness;
- whether the core metric-object identity claim-chain is preserved;
- whether any claim has drifted into overstatement.

## Recommended Use

Use one zip for both sessions.

For SubPro A, paste:

```text
START_HERE_SUBPRO_A_FINITE_MATH_IDENTITY_CHECK_20260708.md
```

For SubPro E, paste:

```text
START_HERE_SUBPRO_E_REDTEAM_IDENTITY_CHECK_20260708.md
```

Each Pro should read only the attached zip for MaoField repository facts.

## Core Claim Under Review

Safe programme translation:

```text
non-identity / unity
  -> measurement-object identity problem
  -> metric-object identity
  -> declared transports / coherence
  -> exact defect certificates
```

Safe sentence:

```text
Metric-object identity is unlicensed unless declared transports and path
closure/coherence conditions hold.
```

The current strongest allowed interpretation is:

- finite exact mathematical artifacts support a controlled metric-object
  identity audit programme;
- they do not prove broad philosophy, broad black-box theory, or empirical
  MaoField claims.

## Included File Groups

1. Current state and rules:
   - `STATE.md`
   - `MD_CATALOG.md`
   - `AGENTS.md`
   - `CLAUDE.md`
   - `rules/CANONICAL_AGENTS.md`

2. Core identity programme:
   - metric-object identity exact-notes index;
   - material-relation/path-closure programme brief;
   - report30/report31/report34/report35/report36 archives and adoption notes
     where relevant.

3. Hard finite mathematical artifacts:
   - v1.3 order-defect formal note;
   - v1.4 exact rational witness note/JSON/script;
   - OI corollary companion;
   - v1.6 quantitative OI formal note/certificate/JSON/script;
   - v1.5 GQ-FCR separate exact note/certificate/JSON/script.

4. D707/D708 internal v0 review artifacts:
   - internal v0 chart/path/cycle defect note;
   - Loop0/Loop3/Loop4/Session4 outputs;
   - report37/report38/report39/report40 archives and adoption notes;
   - D708 report40 RAG record.

5. Control:
   - package manifest;
   - internal SHA256SUMS;
   - this README;
   - SubPro A and SubPro E prompts.

## Boundaries

Still forbidden:

- public-ready / paper-ready / submission-ready / NMI-ready claims;
- MaoField empirical-positive claims;
- full panel, training, checkpoint-loading, inference, or new-loss claims;
- observed residual / interaction / transport / holonomy / gluing / collapse
  field claims;
- dynamic-collapse theory or black-box mechanism solved claims;
- broad ANOVA / dependent-input / projection / sheaf / contextuality /
  path-closure theory claims;
- proof by RAG, JSON, harness, package, prompt, manifest, or model output.

Mode B remains `insufficient_artifact`.

Duplicate/prior-art/triviality risk remains at least `MEDIUM` unless a later
external prior-art review justifies a different label.
