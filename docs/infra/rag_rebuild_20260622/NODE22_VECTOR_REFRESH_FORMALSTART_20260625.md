# Node22 Vector Refresh For Formal Start — 2026-06-25

Date: 2026-06-25 CST (`date` verified on node36 at 11:32 CST).

Purpose: refresh the default MaoField RAG index after formally starting the
debranded finite weighted residual transport / holonomy / operator no-go
direction inside `docs/infra/debranded_residual_transport/`.

## Scope

Node36-owned scope file:

```text
docs/infra/rag_rebuild_20260622/canonical_scope_active_20260625_1132_formalstart.txt
```

Scope count:

```text
366 active canonical markdown files
```

Project scan:

```text
total_files = 7858
markdown = 532
index_markdown_active = 261
digest_only_data = 2133
git_head_at_scan = 5c356a1
```

## Node22 Worker

Node22 was used only as a temporary bge-m3 HTTP embedding worker. Node36 owned
the scope, manifests, logs, verification, backup, and promotion.

Temporary worker service:

```text
endpoint = http://192.168.31.22:18080
model = bge-m3-temp
remote_root = /home/amd/codex-node22/tmp/maofield-rag-vector-20260625_1132_formalstart
```

Final node22 check:

```text
node22_bge_port_18080 = stopped
```

## Build Result

Build log:

```text
docs/infra/rag_rebuild_20260622/rag_build_node22_20260625_1132_formalstart.log
```

Runtime index:

```text
files = 366
chunks = 9330
vectors = 9330
dimension = 1024
```

Promoted default index:

```text
/media/amd/raid1/rag/index/kb.faiss
/media/amd/raid1/rag/index/kb_meta.jsonl
```

Hashes:

```text
kb.faiss      302dee6ba606e7e115a4bd9573038f1e16b68691434af45dc2d9ee70d631d295
kb_meta.jsonl 9abd07a7129dbeac6ca3af74edce3a6e6e3269e9f67b2e71bf092b68457d5443
build_log     e9877ac600cf5426443a94d1f55950956b590758472f22f56434e94063defc56
scope_file    812fb82e16f3142444883980dccec10d5e6861cbf6e782370955e80b382ceb87
scan_summary  3f2aebbd39ab1bcf6ebb64578d151bcb919ff26d262360d11d863eab9be417b0
```

## Boundary

This refresh makes the formal-start materials discoverable through default
RAG. It does not create MaoField empirical evidence.

Strongest allowed local claim from the newly indexed materials:

```text
definitions_and_harness_viable_only
```

Still forbidden:

- full-panel result observed;
- 16-cell aggregate exists;
- residual / interaction / quotient-residual field observed;
- residual transport or holonomy field observed;
- LOSO passed;
- F3 positive;
- glass box broken;
- training or new loss authorized.

RAG locates these files; primary files, code, JSON/JSONL, logs, and verdicts
remain the only claim evidence.
