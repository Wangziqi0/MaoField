# MaoField D706 Pro Decision Brief From RAG, State, And Session Memory

Date verified on node36: 2026-07-06 12:09:37 CST

Status:

```text
ORIENTATION_FOR_PRO_NOT_PROOF
```

This brief exists because the PI clarified that node36 should finish the RAG
and packaging pass, but the main mathematical decision and landing gate should
be delegated to GPT-5.5 Pro. It is compiled from:

- canonical `STATE.md`;
- local RAG smoke results from the D706 node22 vector refresh;
- current-session memory about the PI's intent.

Only repository files, scripts, JSON, DOI/arXiv/publisher pages, and explicit
Pro outputs should be treated as evidence. Session memory is orientation only.

## Current Decision Split

Node36 Codex has prepared the material, but should not unilaterally promote the
new mathematical object.

GPT-5.5 Pro should make the next bounded decision:

```text
APPROVE_NODE36_IMPLEMENT_V1_5_GQ_FCR
PATCH_MATH_DRAFT_FIRST
PIVOT_TO_THREE_CHART_COCYCLE_CERTIFICATE
PIVOT_TO_SUPPORT_RANK_ANOVA_DEFECT
STOP_DUPLICATE_OR_PRIOR_ART_RISK_TOO_HIGH
REQUEST_NODE36_FILES
```

Node36 should execute only after Pro chooses a branch with exact requested
artifacts or blockers.

## What RAG Located

The latest D706 RAG smoke located:

- updated `STATE.md` with live action
  `D706_MATH_LANDING_EXTERNAL_SCAN_PACKAGE_DELIVERED_RAG_SYNCED`;
- `docs/infra/external_math_search/EXTERNAL_MATH_OBJECT_SCAN_D706_20260706.md`;
- `docs/infra/debranded_residual_transport/TWO_CHART_GLUE_CERTIFICATE_V1_5_MATH_LANDING_DRAFT_20260706.md`;
- `docs/infra/recovery/MAOFIELD_D706_EXTERNAL_MATH_LANDING_TASKBOOK_20260706.md`;
- `docs/infra/MAOFIELD_PRO_D706_MATH_LANDING_EXTERNAL_SCAN_PACKAGE_20260706.md`;
- exact-control language for `m_plus`, `m_minus`, and `Delta^2`;
- prior-art boundary language around Robinson consistency radius, cellular
  sheaves, Abramsky-Brandenburger contextuality/global sections, dependent
  ANOVA/Hoeffding-Sobol/Shapley work, and two-projection theory.

RAG is a locator. It does not prove the object.

## What State Says

The current project state says:

- the public Zenodo preprint DOI is `10.5281/zenodo.21190475`;
- the repository/software DOI is `10.5281/zenodo.21157578`;
- the current math landing candidate is
  `Gauge-Quotiented Finite Consistency Radius`;
- Mode B MaoField empirical status remains `insufficient_artifact`;
- duplicate risk remains `MEDIUM`;
- no broad theory claim is authorized.

## Current Session Memory

The PI's current intent, as understood by node36, is:

- do not let node36 freewheel into a new local theory;
- use node36 to gather, package, and keep evidence disciplined;
- give Pro the primary mathematical decision role;
- make this round land in mathematics rather than another conceptual atlas;
- if Pro approves, node36 should implement exact rational artifacts;
- if Pro rejects or patches, node36 should follow the narrower branch.

This memory should not be cited as repository fact. It only explains why the
prompt delegates the main decision to Pro.

## Immediate Candidate

The current candidate object is:

```text
Delta^2_12(s_1,s_2; Gamma_12)
  = min_{gamma in Gamma_12}
      ||rho_1 s_1 - rho_2 s_2 - gamma||^2_w
  = ||(I - P_Gamma)(rho_1 s_1 - rho_2 s_2)||^2_w.
```

Pro should check whether this should be implemented as a narrow exact rational
residual-audit/gauge-quotient specialization of consistency-radius/gluing
ideas.

## Required Conservatism

Pro must not claim:

- broad sheaf theory;
- broad consistency-radius theory;
- broad contextuality theory;
- broad dependent-input ANOVA theory;
- broad noncommuting-projection theory;
- MaoField empirical positive results;
- observed residual, interaction, transport, gluing, or holonomy fields;
- proof from deterministic harnesses or JSON floats.

The correct posture is:

```text
prepare exact finite mathematics first; let claims remain narrow until proof,
script, JSON, and prior-art wording all pass.
```
