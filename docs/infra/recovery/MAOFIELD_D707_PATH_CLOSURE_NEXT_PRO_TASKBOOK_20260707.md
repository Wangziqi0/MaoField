# MaoField D707 Path-Closure Next-Pro Taskbook

Date verified on node36: 2026-07-07 10:07 CST.

## Purpose

Prepare a bounded next Pro review package after D707 v1.6 quantitative OI norm
implementation and Report34 path-closure synthesis.

The package asks Pro to audit and develop:

```text
Material-Relation Path Closure for Finite Metric Objects
```

It must not ask Pro to patch the current Zenodo V2.5 preprint or to claim a
broad theory.

## Inputs

- current `STATE.md` and `MD_CATALOG.md`;
- Report34 raw archive and node36 adoption note;
- material-relation path-closure programme brief;
- v1.3 order-defect formal note;
- D706 OI zero/nonzero corollary companion;
- D707 v1.6 quantitative OI formal note and exact support;
- D706 v1.5 GQ-FCR note and exact support;
- metric-object identity exact-notes index;
- current Zenodo/public-release boundary records;
- raw RAG query outputs for the package.

## Required Pro Decisions

Pro must choose exactly one primary next action:

```text
APPROVE_BOUNDED_CYCLE_DEFECT_FORMAL_NOTE
PATCH_DEFINITIONS_THEN_RECHECK
REJECT_PATH_CLOSURE_AS_TOO_BROAD
REQUEST_NODE36_FILES
```

## Required Pro Checks

1. Does Report34's programme sentence remain framing rather than proof?
2. Does the v1.6 theorem remain finite positive two-way-table mathematics?
3. Are the finite chart definitions `H_c`, `O_c`, `Phi_c`, `U`, `T`, and
   `Delta` coherent enough for a formal note?
4. Is the cycle identity defect `Delta_gamma` composable under explicit finite
   assumptions?
5. Is the telescoping identity for `Delta_{gamma,n}` correct?
6. Does the black-box audit framing solve only metric-object identity
   licensing, not mechanism interpretability?
7. Which one bounded exact object should node36 implement next?

## Forbidden Outputs

Pro must not output:

- MaoField empirical positive result;
- full panel, checkpoint loading, inference, training, or new loss;
- observed residual, interaction, transport, holonomy, gluing, or collapse
  field;
- broad dynamic-collapse, sheaf, contextuality, dependent-input ANOVA, or
  projection theory;
- proof by deterministic harness, JSON, or model output;
- current Zenodo V2.5 preprint patch text;
- claims that philosophy proves the mathematics or that mathematics proves
  the whole philosophy.

## Completion Criteria

This task is complete when:

- a zero-context Pro prompt is written;
- a light zip package is copied to node19 desktop;
- package manifest and hashes are recorded;
- RAG is refreshed through node22 one-shot vector service;
- `STATE.md`, `MD_CATALOG.md`, and git are updated.
