# D708 Extra SubPro A/E Identity Check Taskbook

## Purpose

Prepare a bounded extra-Pro review package after report40 accepted the D707/D708
internal v0 note only as:

```text
ACCEPT_INTERNAL_V0_AS_LOCAL_NOTE_ONLY
KEEP_INTERNAL_ONLY
```

The package is for two extra manual Pro sessions:

- SubPro A: finite-math verification of definitions, composition, telescoping,
  and norm bounds.
- SubPro E: red-team review of prior-art/triviality/overclaim risk and the
  metric-object identity claim-chain.

This is not a request to write a paper, upgrade the Zenodo preprint, claim NMI
readiness, or start a black-box audit.

## Core Question

The extra reviewers should answer whether the current artifacts preserve the
core MaoField mathematical translation:

```text
non-identity / unity
  -> measurement-object identity problem
  -> metric-object identity
  -> declared transports / coherence
  -> exact defect certificates
```

Current safest sentence:

```text
Metric-object identity is unlicensed unless declared transports and path
closure/coherence conditions hold.
```

The reviewers must distinguish:

- finite theorems and exact certificates;
- programme-level mathematical framing;
- empirical claims, which remain blocked.

## Inputs

Package inputs should include:

- current `STATE.md`, `MD_CATALOG.md`, and rule snapshots;
- metric-object identity index and programme brief;
- v1.3 order-defect note, v1.4 exact witness, and exact witness script/JSON;
- OI corollary companion and v1.6 quantitative OI note/certificate/script/JSON;
- v1.5 GQ-FCR separate exact note/certificate/script/JSON;
- D707 internal v0 chart/path/cycle defect note;
- D707 loop outputs for Loop0/Loop3/Loop4/Session4;
- report37/report38/report39/report40 and their adoption notes;
- current D708 report40 RAG record.

## Required Verdicts

SubPro A must choose exactly one:

```text
PASS_FINITE_MATH_WITH_NOTES
PATCH_REQUIRED_FINITE_MATH
BLOCKED_MISSING_ARTIFACTS
```

SubPro E must choose exactly one:

```text
PASS_REDTEAM_WITH_CLAIM_GUARDS
PATCH_REQUIRED_OVERCLAIM_OR_PRIOR_ART
BLOCKED_PRIOR_ART_OR_TRIVIALITY_RISK
BLOCKED_MISSING_ARTIFACTS
```

## Boundary

Still forbidden:

- public-ready / paper-ready / submission-ready / NMI-ready claims;
- MaoField empirical-positive claims;
- full panel, training, checkpoint-loading, inference, or new-loss claims;
- observed residual / interaction / transport / holonomy / gluing / collapse
  field claims;
- dynamic-collapse theory or black-box mechanism solved claims;
- broad ANOVA / dependent-input / projection / sheaf / contextuality /
  path-closure theory claims;
- proof by RAG, JSON, harness, package, prompt, manifest, or model output.

Mode B remains `insufficient_artifact`.

Duplicate/prior-art/triviality risk remains at least `MEDIUM` unless SubPro E
performs and documents stronger external prior-art review.

## Node36 Handling

Node36 must package only necessary files. Do not include old zips, checkpoints,
model weights, large experiment data, or whole recovery archives.

The package is a review input, not evidence authority. Evidence authority remains
with primary files, proofs, scripts, exact certificates, and explicit Pro
verdicts.
