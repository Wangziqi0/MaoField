# MaoField D704 Metric-Identity Phase II Pro Taskbook

Date verified on node36: 2026-07-04 21:38 CST.

Authority: node36 canonical MaoField repository.

## Purpose

Build a light but complete zero-context GPT-5.5 Pro polling package for the
next MaoField research phase after the formal Zenodo preprint and the
`maofield_metric_identity_programme.md` adoption.

The target research problem is not another proof-check of the already-published
finite order-defect note. The target is the next mathematical programme:

```text
When can metric outputs across LLM evaluation charts be treated as the same
measurement object?
```

The package must preserve the exact finite order-defect result as the first
certificate/wedge while asking Pro to search for the next rigorous object:
metric-object identity, metric-form fetishism, chart-dependent metric outputs,
transport/invariance conditions, defect certificates, audit-order instability,
and a residual metric audit protocol.

## Current Evidence State

- Public Open-MaoField repository exists at `https://github.com/Wangziqi0/Open-MaoField`.
- Public repository HEAD recorded in canonical state: `961acf4`.
- Public tag/release recorded in canonical state: `v1.0.0`.
- Formal Zenodo preprint DOI recorded in canonical state: `10.5281/zenodo.21190475`.
- Repository/software Zenodo DOI recorded in canonical state: `10.5281/zenodo.21157578`.
- Canonical private MaoField HEAD at package preparation start: `3753d15`.
- Latest RAG refresh tag: `20260704_2045_metric_identity_programme`.
- RAG active files: `594`.
- RAG chunks: `12034`.
- Mode B MaoField empirical status remains `insufficient_artifact`.
- Duplicate risk remains `MEDIUM`.

## Package Weighting

The package should bias Pro's attention as follows:

1. New Phase II metric-identity programme and adoption note.
2. RAG search outputs showing how the current index locates the new mainline.
3. Previous Pro reports that reframed the finite theorem as a programme wedge.
4. V2.5 final-synthesis preprint result, release gate, Zenodo/publication records.
5. Exact finite order-defect theorem, exact rational witness, and deterministic harness boundary.
6. Canonical project state and catalog.
7. Negative/deflated old MaoField context only as motivation, not evidence of a positive empirical claim.

## Required Boundaries

Pro must not claim:

- MaoField empirical positive result;
- full panel has run;
- training/new loss/checkpoint inference is authorized or completed;
- observed residual, interaction, transport, holonomy, or metric field in MaoField data;
- glass box broken;
- F3 positive or LOSO passed;
- broad new ANOVA, dependent-input decomposition, or projection theory;
- deterministic harness or JSON floats prove the theorem;
- Zenodo/GitHub publication equals peer review.

The programme may use philosophical language only to motivate technical object
selection. All claims must be carried by finite definitions, theorems, exact
certificates, audits, literature comparison, or explicitly marked conjectures.

## Pro Polling Protocol

Ask Pro to work in rounds:

1. Package intake and evidence-boundary check.
2. Reconstruct the current minimal theorem and why it is only a wedge.
3. Formalize the metric-object identity problem for evaluation charts.
4. Search for adjacent mathematical and LLM-evaluation prior art.
5. Propose the smallest publishable next mathematical object or counterexample.
6. Design a certificate library and residual metric audit protocol v0.1.
7. Explicitly list missing files/data and request them from node36 Codex if needed.

## Data Request Policy

Pro is allowed and encouraged to ask for additional local files from node36
Codex if the package is insufficient. Acceptable request format:

```text
REQUEST_TO_NODE36_CODEX:
- exact path or file category
- why it is needed
- what claim it would test
- whether it is blocking or optional
```

Node36 should then provide only the needed primary files or RAG snippets, not a
large uncontrolled repository dump.

## Deliverables Expected From Pro

Pro should return:

- a strict evidence verdict;
- a clean formal definition draft for evaluation charts and metric-object identity;
- one or more candidate theorem/counterexample paths;
- a literature/duplicate-risk map;
- a list of exact certificates to construct next;
- a residual metric audit protocol v0.1;
- a decision on whether the next step is proof, literature audit, certificate coding, or empirical protocol design.

## Local Build Note

Package assembly must run in node36 SSD scratch and promote only durable records
back to canonical. The package itself is for node19 desktop delivery.
