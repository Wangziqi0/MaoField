# Boundary Guards - Loop 4 - 2026-07-07

Status: `LOOP4_BOUNDARY_GUARDS`

This file records the forbidden-claim guards for the finite cycle-defect and telescoping artifacts.

## Allowed Statement

Cycle-iteration defect is finite path-defect propagation under the declared Loop 3 chart, edge, path, and norm data.

Allowed wording:

```text
Given declared finite transports `U_gamma` and `T_gamma`, the iterated defect
`Delta_{gamma,n}` telescopes as a finite sum of transported one-cycle defects.
Finite-horizon quantities such as `CID_gamma(S)` and `CIC_N(gamma,S)` can be
computed or bounded under explicit norm-growth and image-control hypotheses.
```

## Forbidden Upgrades

| Forbidden upgrade | Status | Safe replacement |
|---|---|---|
| Cycle-iteration defect is model collapse. | BLOCKED | It is finite path-defect propagation under declared transports. |
| The telescoping identity is dynamic-collapse theory. | BLOCKED | It is a finite linear-algebra identity for repeated traversal of one declared cycle. |
| `Delta_gamma != 0` is observed collapse. | BLOCKED | It permits only finite-horizon defect computation or bounding. |
| `U_gamma` or `T_gamma` is an observed transport field. | BLOCKED | They are declared transports in the finite chart system. |
| The cycle defect is observed holonomy, gluing, residual, or transport field in MaoField data. | BLOCKED | No such empirical field is asserted by Loop 4. |
| The note explains a black-box mechanism. | BLOCKED | It gives a metric-object identity audit calculus candidate only. |
| The note is an empirical MaoField positive result. | BLOCKED | It contains local finite linear algebra and definitions only. |
| JSON, scripts, RAG, prompts, or reports prove the theorem. | BLOCKED | Primary text proofs carry the finite algebra; support artifacts only locate or check. |
| The path-closure sentence is a theorem. | BLOCKED | `Identity is not naming; identity is path closure under declared material relations` remains programme framing. |
| The project is NMI-ready, paper-ready, or public-readiness upgraded by Loop 4. | BLOCKED | Loop 4 creates local artifacts for Pro review only. |
| The note expands v1.6 into broad ANOVA, sheaf, contextuality, dependent-input, or projection theory. | BLOCKED | v1.6 remains finite two-way-table two-projection commutator geometry only. |

## Replacement Discipline

- Use `finite cycle defect`, `iterated defect`, `finite-horizon propagation`, and `declared transports`.
- Do not use `observed collapse`, `collapse mechanism`, `dynamic collapse`, `empirical positive`, or `black-box solved`.
- Do not infer asymptotic behavior from `CIC_N`; it is defined over a finite horizon `1<=n<=N`.
- Do not replace `||Delta_gamma||` by `||Delta_gamma|_S||` unless image control such as `U_gamma^j(S) subset S` is stated.

## Prior-Art And Claim-Hygiene Carryover

Loop 0's adjacent-contradiction gate remains active: validity, construct validity, estimands, benchmark sensitivity, IRT calibration, referential security, judge/rubric drift, and model collapse must be acknowledged before novelty language. Loop 4 makes no novelty claim.

The retracted first-dialectical-materialism, first-reflexive-AI, paradigm-shift, C1/C2/C3, C4 decoupling-positive, and diversity-vs-PPL decoupling-positive claims remain blocked from revival.
