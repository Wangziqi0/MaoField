# MaoField D705 Finite Glue Object Taskbook

Date verified on node36: 2026-07-05 10:35:34 CST

## Task

Prepare the next GPT-5.5 Pro package after Report26. The package must move from
object-atlas discussion to a bounded mathematical construction target:

```text
FINITE_GLUE_OBJECT_FIRST
```

## Goal

Ask Pro to determine whether a minimal exact rational finite gluing obstruction
certificate can be defined, proved, and implemented in the current MaoField /
Open-MaoField style.

The target is not a broad theory. The target is a small finite object:

```text
two local charts + overlap + local residual sections + restriction maps
+ allowed local nuisance/gauge + gauge-minimized mismatch
```

## Required Inputs

The package must include:

- Report26 and this adoption chain;
- Report25 and the metric-identity programme chain;
- current `STATE.md`, `MD_CATALOG.md`, `AGENTS.md`, `CLAUDE.md`;
- finite order-defect proof, exact 2x2 witness, exact witness script/JSON, and
  deterministic harness v1.3;
- older residual transport / holonomy / gluing notes, scripts, and JSON files
  as historical scaffolding;
- raw node36 search sidecars answering Report26's optional requests;
- forbidden-claim and boundary context sufficient to prevent overclaiming.

## Pro Must Decide

Pro must choose exactly one:

```text
DESIGN_TWO_CHART_GLUE_CERTIFICATE
PATCH_DEFINITIONS_BEFORE_GLUE
BUILD_OI_AND_PRODUCT_CONTROLS_FIRST
REQUEST_NODE36_FILES
STOP_BRANCH_INSUFFICIENT_RIGOR
```

## Construction Constraints

- Zero GPU.
- Exact rational arithmetic preferred.
- Any float harness is regression support only.
- Positive and negative controls required before any public mathematical claim.
- Every object must be finite-dimensional and file-backed.
- Every analogy to sheaf, holonomy, or gluing must be labeled as finite
  analogy unless a theorem is actually proved.

## Prohibited Outputs

Pro must not draft a paper body in this round.

Pro must not claim:

- MaoField empirical positive evidence;
- observed residual/transport/holonomy/gluing field;
- broad ANOVA, sheaf, holonomy, dependent-input, or projection theory;
- training, inference, new loss, full panel, F3-positive, LOSO-passed, or
  checkpoint result;
- that JSON floats or deterministic harnesses prove a theorem.

## Node36 Follow-Up After Pro

If Pro returns `DESIGN_TWO_CHART_GLUE_CERTIFICATE`, node36 can turn the design
into:

1. a formal note;
2. an exact rational script;
3. JSON certificate output;
4. Markdown verification;
5. fail-closed controls;
6. RAG refresh and git commit.

If Pro returns any blocker, node36 should record the blocker in `STATE.md` and
avoid implementation until the PI chooses a branch.
