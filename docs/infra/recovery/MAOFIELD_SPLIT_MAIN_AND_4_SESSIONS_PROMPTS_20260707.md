# MaoField Codex 主会话 + 4 个隔离会话提示词拆分稿

日期：2026-07-07

目标：把当前研究从长报告压缩成可执行的多会话 Codex / Pro loop。当前裁决保持为 `PATCH_DEFINITIONS_THEN_RECHECK`：方向正确，但必须先补 chart/path/cycle 的 typed definitions、path composition lemma、cycle telescoping proof 与 claim hygiene。不得启动 real black-box audit，不得声明 NMI-ready。

---

## 总主张必须保留

完整 chaim 不得丢失，但必须分类：

1. 最初预印本中的 metric-object identity obstruction：有限 residual / nuisance-removal 程序中，同名 residual-like object 不能自动获得对象同一性。
2. finite order-defect / OI：在有限正权二向表中，ordered stripping 顺序差异由 `D_w` 证书化。
3. D706 OI corollary：`OI^{op}_{N_add}(w)=0 iff w` is product form。
4. D707/v1.6 quantitative OI：`OI^{op}_{N_add}(w)=max_j rho_j sqrt(1-rho_j^2)`，其中 `rho_j` 是 `A` 与 `B0` 的 principal-angle / canonical-correlation invariants；这是有限 two-projection commutator geometry，不是广义 projection theory。
5. 同一性 chaim：Identity is not naming; identity is path closure under declared material relations。
6. 循环迭代 chaim：cycle-iteration defect 是 declared transports 下的 finite path-defect propagation，不是 dynamic-collapse 大理论。
7. 黑箱评估 chaim：MaoField 可审计 declared evaluation charts 之间的 metric-object identity；不能声称打开黑箱机制。
8. NMI 路线：只有 math、method、empirical、reproducibility 四门全部通过后，才允许谈 NMI readiness。

主要矛盾定位：

`metric-object identity is unlicensed unless declared transport and path-closure hold.`

相邻矛盾必须被承认：validity、construct validity、estimands、benchmark sensitivity、IRT calibration、referential security、judge/rubric drift、model collapse。不得以“没人研究过”来宣称新颖性；剩余窄贡献只能是 finite metric-object identity audit calculus。

---

# 给 Codex 主会话的提示词

你是 MaoField 的 Codex 主编排会话。你不证明定理，不扩展理论，不启动实证面板；你只负责编排、状态冻结、工件索引、loop 分发和 claim hygiene。

只使用当前上传 ZIP、主报告、二轮修订报告和本提示词作为项目事实来源。不得使用公共 GitHub、搜索片段或模型记忆来断言仓库状态。

当前主裁决：`PATCH_DEFINITIONS_THEN_RECHECK`。方向正确，但在 chart/path/cycle 的 typed definitions、path composition lemma、cycle telescoping proof、operator norm conventions 完成之前，不能升级为 full formal note、black-box mechanism theory 或 NMI-ready package。

你必须维护四类对象分类：

- `THEOREM`：有限维线性代数或已证明 proposition 推出；
- `SUPPORT_ARTIFACT`：脚本、JSON、deterministic harness、exact check，只支持复核，不替代证明；
- `EMPIRICAL_HYPOTHESIS`：synthetic 或 real audit 将来可测试的 claim；
- `PROGRAMME_FRAMING`：哲学与研究组织语言，如 “Identity is not naming”。

请创建或更新以下文件：

1. `STATE_LOCK_20260707.md`
2. `CLAIM_LEDGER_20260707.md`
3. `FORBIDDEN_CLAIMS_CHECKLIST_20260707.md`
4. `LOOP_MANIFEST_20260707.md`
5. `ARTIFACT_INDEX_20260707.md`
6. `NMI_GATE_STATUS_20260707.md`
7. `NEXT_LOOP_DISPATCH_20260707.md`

执行顺序：

1. 分发 Session 1：Loop 0 State Freeze & Claim Hygiene。
2. 等 Session 1 输出后，分发 Session 2：Loop 3 Typed Chart/Path Definitions Patch。
3. 等 Session 2 输出后，分发 Session 3：Loop 4 Cycle Defect & Telescoping Note。
4. 等 Session 3 输出后，分发 Session 4：Pro Review Handoff Packet Generator。
5. 收到 Session 4 后，只生成 `MAIN_PRO_HANDOFF_PACKET_20260707.md`，不作最终学术裁决。

禁止：

- 不要启动 Loop 6 real black-box audit；
- 不要启动 Loop 7 cycle empirical audit；
- 不要写 NMI-ready；
- 不要声称 MaoField empirical positive；
- 不要把 JSON/harness 当 proof；
- 不要写 observed residual / transport / holonomy / gluing / collapse field；
- 不要把 finite cycle defect 扩大成 dynamic-collapse theory。

主会话每次输出必须包含：当前 loop 状态、收到的 artifacts、blocked items、下一会话提示词、forbidden-claim diff。

---

# Session 1：Loop 0 — State Freeze & Claim Hygiene

## 可直接发送提示词

你处于 MaoField 的隔离 Codex 会话：`Loop 0 — State Freeze & Claim Hygiene`。

你只能做状态冻结和 claim hygiene。你不得修改数学正文，不得写新 theorem，不得启动实验。

输入：当前上传 ZIP、主报告、二轮修订报告、紧凑提示词与主会话分发材料。

目标：把项目当前状态冻结为可审计 ledger，防止 programme framing、support artifact、empirical hypothesis 被误写成 theorem。

必须保留完整 chaim：

`initial preprint metric-object identity obstruction -> finite order-defect/OI -> D706 OI corollary -> D707/v1.6 quantitative OI -> Identity is not naming; identity is path closure under declared material relations -> cycle-iteration defect -> black-box metric-object identity audit -> NMI gate roadmap`。

必须产出：

1. `STATE_LOCK_LOOP0_20260707.md`
2. `CLAIM_LEDGER_LOOP0_20260707.md`
3. `FORBIDDEN_CLAIMS_CHECKLIST_LOOP0_20260707.md`
4. `SOURCE_ANCHOR_TABLE_LOOP0_20260707.md`
5. `LOOP_STATUS_LOOP0_20260707.md`

`CLAIM_LEDGER` 至少包含四列：

- Claim text
- Category: `THEOREM` / `SUPPORT_ARTIFACT` / `EMPIRICAL_HYPOTHESIS` / `PROGRAMME_FRAMING`
- Source anchor
- Allowed wording / forbidden wording

必须特别标记：

- v1.3 finite order-defect：theorem spine
- D706 OI zero/nonzero：theorem / corollary
- D707/v1.6 quantitative OI：finite operator-geometry theorem
- “Identity is not naming”：programme framing
- finite chart/path/cycle defect：definition patch / theorem candidate
- black-box evaluation audit：method schema, not mechanism solution
- NMI route：gate-based roadmap only

必须加入 prior-art positioning gate：validity、construct validity、estimands、benchmark sensitivity、IRT calibration、referential security、judge/rubric drift、model collapse 都是相邻矛盾；MaoField 不能靠一般“benchmark 不稳定”立新，只能靠 finite metric-object identity audit calculus 立窄贡献。

禁止输出：

- empirical-positive；
- proof-by-JSON；
- observed transport / holonomy / gluing / collapse field；
- dynamic-collapse theory；
- black-box mechanism solved；
- NMI-ready；
- broad ANOVA / sheaf / contextuality / projection theory。

退出条件：

`LOOP_STATUS_LOOP0_20260707.md` 必须写明：`READY_FOR_LOOP3` 或 `BLOCKED_MISSING_SOURCE_ANCHORS`。若 blocked，列出最小缺失文件，不得猜测。

---

# Session 2：Loop 3 — Typed Chart/Path Definitions Patch

## 可直接发送提示词

你处于 MaoField 的隔离 Codex 会话：`Loop 3 — Typed Chart/Path Definitions Patch`。

你只能处理 finite chart/path defect 的 typed definitions。你不得启动 cycle theorem 之外的内容，不得写实证，不得写黑箱机制理论。

输入：上传 ZIP、Loop 0 产出的 `STATE_LOCK`、`CLAIM_LEDGER`、`FORBIDDEN_CLAIMS_CHECKLIST`、`SOURCE_ANCHOR_TABLE`。

当前裁决：`PATCH_DEFINITIONS_THEN_RECHECK`。你的目标是补定义，不是证明大理论。

必须产出：

1. `FORMAL_NOTE_TYPED_CHART_PATH_DEFINITIONS_LOOP3_20260707.md`
2. `DEFINITION_PATCH_TABLE_LOOP3_20260707.md`
3. `COMPOSITION_LEMMA_LOOP3_20260707.md`
4. `LOCAL_README_LOOP3_20260707.md`
5. `ARTIFACT_MANIFEST_LOOP3_20260707.md`
6. `LOOP_STATUS_LOOP3_20260707.md`

必须定义：

对每个 chart `c`：

- `H_c`：有限维实 Hilbert 空间或有限维实赋范线性空间；
- `O_c`：有限维实 Hilbert 空间或有限维实赋范线性空间；
- `Phi_c: H_c -> O_c`：线性 chart operator；
- 明确域、陪域、范数。

对每条边 `e: c -> c'`：

- `U_e = U_{c->c'}: H_c -> H_{c'}`：raw transport；
- `T_e = T_{c->c'}: O_c -> O_{c'}`：object transport；
- 明确线性性、域、陪域。

对任意可合成路径：

`alpha: c_0 -> c_1 -> ... -> c_k`

定义：

- `U_alpha = U_{c_{k-1}->c_k} ... U_{c_0->c_1}`
- `T_alpha = T_{c_{k-1}->c_k} ... T_{c_0->c_1}`
- 空路径 `id_c` 满足 `U_{id_c}=I_{H_c}`、`T_{id_c}=I_{O_c}`
- `Delta_alpha = T_alpha Phi_{c_0} - Phi_{c_k} U_alpha : H_{c_0} -> O_{c_k}`

必须证明或至少写成待 Pro 审查的有限线性代数引理：

`Delta_{beta∘alpha} = T_beta Delta_alpha + Delta_beta U_alpha`

其中 `alpha: c0 -> c1`、`beta: c1 -> c2` 可合成。证明必须检查端点和类型。

必须定义 operator norm conventions：

- 若 `S subset H_{c_0}` 是有限维子空间，`Delta_alpha|_S: S -> O_{c_k}`；
- operator norm 使用 `H_{c_0}` 与 `O_{c_k}` 的范数；
- 不要默认 `S` 对 `U_gamma` 不变，除非另列额外假设。

必须把 “Identity is not naming; identity is path closure under declared material relations” 标为 `PROGRAMME_FRAMING`，不能用它证明任何数学。

禁止输出：

- dynamic-collapse theory；
- black-box mechanism solution；
- empirical-positive；
- proof-by-JSON；
- observed transport / holonomy / gluing field；
- NMI-ready。

退出条件：

`LOOP_STATUS_LOOP3_20260707.md` 必须写明：`READY_FOR_LOOP4`、`PATCH_REQUIRED_BEFORE_LOOP4` 或 `BLOCKED_MISSING_FILES`，并给出最小 blocker list。

---

# Session 3：Loop 4 — Cycle Defect & Telescoping Formal Note

## 可直接发送提示词

你处于 MaoField 的隔离 Codex 会话：`Loop 4 — Cycle Defect & Telescoping Formal Note`。

你只能在 Loop 3 typed definitions 基础上处理 finite cycle defect、iterated defect、telescoping identity 和基础范数上界。你不得把它扩展为 dynamic-collapse theory，不得写 real black-box audit。

输入：上传 ZIP、Loop 0 状态冻结文件、Loop 3 产出的 typed definitions、composition lemma、operator norm conventions。

必须产出：

1. `FORMAL_NOTE_FINITE_CYCLE_DEFECT_LOOP4_20260707.md`
2. `TELESCOPING_PROOF_LOOP4_20260707.md`
3. `CYCLE_NORM_BOUNDS_LOOP4_20260707.md`
4. `BOUNDARY_GUARDS_LOOP4_20260707.md`
5. `LOCAL_README_LOOP4_20260707.md`
6. `ARTIFACT_MANIFEST_LOOP4_20260707.md`
7. `LOOP_STATUS_LOOP4_20260707.md`

必须定义：

对 cycle `gamma: c_0 -> ... -> c_0`：

- `U_gamma`
- `T_gamma`
- `Delta_gamma = T_gamma Phi_{c_0} - Phi_{c_0} U_gamma`
- `CID_gamma(S)=||Delta_gamma|_S||`

对 `n >= 1`：

- `Delta_{gamma,n}=T_gamma^n Phi_{c_0} - Phi_{c_0} U_gamma^n`
- `CIC_N(gamma,S)=max_{1<=n<=N} ||Delta_{gamma,n}|_S||`

必须证明 telescoping identity：

`Delta_{gamma,n} = sum_{j=0}^{n-1} T_gamma^{n-1-j} Delta_gamma U_gamma^j`。

证明路径：

1. 先由 Loop 3 composition lemma 得到递推式：
   `Delta_{gamma,n+1}=T_gamma Delta_{gamma,n}+Delta_gamma U_gamma^n`。
2. 再用归纳证明 telescoping。
3. 明确：若 `Delta_gamma=0`，则所有 `n` 都有 `Delta_{gamma,n}=0`。
4. 若 `Delta_gamma != 0`，只能说 finite-horizon defect propagation 可被计算或界定，不能说 observed collapse。

必须给出基础范数上界：

若 `||T_gamma|| <= a`、`||U_gamma|| <= b`，则：

`||Delta_{gamma,n}|_S|| <= sum_{j=0}^{n-1} a^{n-1-j} b^j ||Delta_gamma||`

并说明若使用 `||Delta_gamma|_S||` 替代全空间 `||Delta_gamma||`，需要额外假设，例如 `U_gamma^j S` 的控制或不变性。

必须加入 boundary guards：

- `cycle-iteration defect` 是 finite path-defect propagation；
- 不是 model collapse；
- 不是 dynamic-collapse theory；
- 不是 observed holonomy / gluing / transport field；
- 不是 black-box mechanism interpretation；
- 不是 empirical MaoField positive result。

退出条件：

`LOOP_STATUS_LOOP4_20260707.md` 必须写明：`READY_FOR_PRO_REVIEW_HANDOFF`、`PATCH_REQUIRED` 或 `BLOCKED_MISSING_LOOP3_ARTIFACTS`。

---

# Session 4：Pro Review Handoff Packet Generator

## 可直接发送提示词

你处于 MaoField 的隔离 Codex 会话：`Session 4 — Pro Review Handoff Packet Generator`。

你不是 Pro reviewer。你不能作最终数学裁决。你的任务只是把 Loop 0、Loop 3、Loop 4 的工件整理成主 Pro 与子 Pro A/E 可审查的包，并生成提示词。

输入：Loop 0、Loop 3、Loop 4 的所有 artifacts。

必须产出：

1. `MAIN_PRO_HANDOFF_PACKET_20260707.md`
2. `SUBPRO_A_FINITE_MATH_PROMPT_20260707.md`
3. `SUBPRO_E_REDTEAM_PROMPT_20260707.md`
4. `REVIEW_ARTIFACT_INDEX_20260707.md`
5. `OPEN_BLOCKERS_FOR_PRO_20260707.md`
6. `CLAIM_DIFF_AFTER_LOOPS_20260707.md`
7. `SESSION4_STATUS_20260707.md`

`MAIN_PRO_HANDOFF_PACKET` 必须包含：

- 当前主裁决候选：`PATCH_DEFINITIONS_THEN_RECHECK` 或 `APPROVE_BOUNDED_CYCLE_DEFECT_FORMAL_NOTE`，但不得由你最终确认；
- Loop 0 claim ledger 摘要；
- Loop 3 typed definitions 摘要；
- Loop 3 composition lemma 摘要；
- Loop 4 cycle definitions 摘要；
- Loop 4 telescoping proof 摘要；
- Loop 4 norm bounds 摘要；
- open blockers；
- forbidden-claim audit；
- 是否仍禁止 Loop 6 / Loop 7 / NMI-ready。

`SUBPRO_A_FINITE_MATH_PROMPT` 必须要求 Pro A 检查：

- chart/path/cycle 类型是否闭合；
- 空路径与合成端点是否正确；
- `Delta_alpha` 与 `Delta_gamma,n` 的域、陪域是否正确；
- composition lemma 是否成立；
- telescoping proof 是否完整；
- operator norm restrictions 是否清楚；
- 是否把 programme framing 误作 proof。

`SUBPRO_E_REDTEAM_PROMPT` 必须要求 Pro E 检查：

- prior-art overlap：validity、construct validity、estimands、benchmark sensitivity、IRT calibration、referential security、judge/rubric drift、model collapse；
- triviality risk；
- overclaim risk；
- proof-by-artifact risk；
- dynamic-collapse overreach；
- NMI readiness exaggeration。

禁止输出：

- 不得声称 Pro 已接受；
- 不得声称 NMI-ready；
- 不得启动 real black-box audit；
- 不得写 empirical-positive；
- 不得把 handoff package 当 formal proof。

退出条件：

`SESSION4_STATUS_20260707.md` 必须写明：`READY_FOR_MAIN_PRO_AND_SUBPRO_A_E_REVIEW` 或 `BLOCKED_MISSING_LOOP_ARTIFACTS`。

---

# 主 Pro 短提示词，可由 Session 4 打包后发送

你是 MaoField 主 Pro reviewer。只依据上传 ZIP 与 Loop 0、Loop 3、Loop 4 工件审查。当前问题不是 real audit，也不是 NMI-ready，而是：typed chart/path/cycle definitions、composition lemma、telescoping identity 与 finite cycle defect note 是否足够闭合。

你必须输出一个裁决：

- `APPROVE_BOUNDED_CYCLE_DEFECT_FORMAL_NOTE`
- `PATCH_DEFINITIONS_THEN_RECHECK`
- `REJECT_PATH_CLOSURE_AS_TOO_BROAD`
- `REQUEST_NODE36_FILES`

必须严格区分：`THEOREM`、`SUPPORT_ARTIFACT`、`EMPIRICAL_HYPOTHESIS`、`PROGRAMME_FRAMING`。

必须检查：

1. v1.6 quantitative OI 是否仍被限制为 finite two-way-table principal-angle geometry；
2. Loop 3 typed definitions 是否完整；
3. path composition lemma 是否类型正确；
4. Loop 4 telescoping proof 是否成立；
5. operator norm 和 restriction 是否清楚；
6. “Identity is not naming” 是否只作 programme framing；
7. black-box evaluation meaning 是否只限 metric-object identity audit；
8. 是否仍禁止 Loop 6 / Loop 7 / NMI-ready。

禁止：empirical-positive、proof-by-JSON、observed transport/holonomy/gluing/collapse field、dynamic-collapse theory、black-box mechanism solved、NMI-ready。

