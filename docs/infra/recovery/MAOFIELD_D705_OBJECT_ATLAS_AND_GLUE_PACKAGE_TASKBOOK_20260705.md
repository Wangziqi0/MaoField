# MaoField D705 Object Atlas And Gluing Package Taskbook

Date verified on node36: 2026-07-05 10:06:21 CST

## Task

Revise the next GPT-5.5 Pro package so the model does not only decide between
pre-listed branches, but also performs a controlled mathematical-object atlas
pass over the RAG-retrieved MaoField claim/object chain.

## Reason

The previous D705 decision package correctly carried Report25 and its default
recommendation:

```text
FORMALIZE_DEFINITIONS_FIRST
```

The user now asks that the next Pro round also inspect all plausible
RAG-retrieved mathematical objects and consider whether new objects can be
created by finite gluing / pasting constructions.

## Required Expansion

The revised package must include raw RAG query outputs for:

- metric-object identity;
- all possible mathematical object candidates;
- quotient residual / finite ANOVA / hypercube / interaction field;
- residual transport / holonomy / gluing / sheaf-like obstruction;
- order-defect extensions and certificate library;
- LLM measurement black-box / metric-form fetishism.

The revised prompt must require Pro to produce:

1. an object atlas from the RAG hits;
2. a claim-chain map separating source-supported objects from speculative ones;
3. finite gluing / pasting candidates;
4. proof obligations and certificate requirements;
5. hard forbidden-claim checks;
6. a final decision between definition-first, object-atlas-first, gluing-first,
   certificate-library-first, duplicate-audit-first, protocol-first, or stop.

## Boundary

Creativity is allowed only as labeled mathematical proposal. It must not be
upgraded into evidence, proof, empirical MaoField result, broad theory, or
publication-ready claim.

No GPU training or empirical panel work is authorized.

