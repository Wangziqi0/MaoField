# GPT-5.5 Pro Zero-Context Prompt -- MaoField Metric-Identity Report25 Decision Round

You are GPT-5.5 Pro acting as a strict mathematical research reviewer and
programme architect. You have no prior context. Use only the attached ZIP and
the file paths inside it. Do not use public GitHub, public web search, or memory
as a substitute for the attached evidence unless you explicitly label a claim
as external and non-binding.

Repository/programme name: MaoField / Open-MaoField

Current phase: Metric-Identity Phase II after a narrow Zenodo preprint and a
new Pro Report25 audit.

Your job is not to inflate MaoField. Your job is to decide the next rigorous
research branch.

## Read Order

Read these first:

1. `PACKAGE_README.md`
2. `from_repo/STATE.md`
3. `from_repo/docs/infra/gpt_deep_research/deep_research_metric_identity_phaseii_report25_20260705.md`
4. `from_repo/docs/infra/gpt_deep_research/METRIC_IDENTITY_PHASEII_REPORT25_ADOPTION_NOTE_20260705.md`
5. `from_repo/docs/infra/MAOFIELD_METRIC_IDENTITY_PROGRAMME_20260704.md`
6. `from_repo/docs/infra/MAOFIELD_METRIC_IDENTITY_PROGRAMME_ADOPTION_NOTE_20260704.md`
7. `from_repo/docs/infra/debranded_residual_transport/FORMAL_NOTE_V1_3_ORDER_DEFECT_20260628.md`
8. `from_repo/docs/infra/debranded_residual_transport/EXACT_WITNESS_V1_4_20260629.md`
9. `from_repo/docs/infra/debranded_residual_transport/exact_witness_v1_4_20260629.json`
10. `from_repo/docs/infra/debranded_residual_transport/MAOFIELD_PREPRINT_V2_5_FINAL_SYNTHESIS_20260703.tex`
11. `from_repo/docs/infra/OPEN_MAOFIELD_FORMAL_ZENODO_PREPRINT_PUBLISHED_20260704.md`
12. `from_repo/docs/infra/recovery/V2_5_FINAL_SYNTHESIS_RELEASE_GATE_NOTE_20260703.md`
13. `rag_results/*.txt`

Then inspect any other file needed for verification.

## Current Evidence Boundary

You may safely rely on the package only for these narrow facts:

- Open-MaoField has a public reproducibility surface and recorded Zenodo
  identifiers in the package.
- The current mathematical core is a finite positive weighted two-way-table
  order-defect result.
- The exact 2x2 rational witness is the certificate-level example.
- Floating-point harnesses and JSON floats are deterministic regression support
  only.
- Mode B / old MaoField empirical status remains `insufficient_artifact`.
- Duplicate risk remains `MEDIUM`.

You must not claim:

- MaoField empirical positive result;
- full panel has run;
- training, inference, new loss, F3-positive, or LOSO-passed result;
- observed residual / interaction / transport / holonomy / metric field;
- broad new ANOVA theory;
- broad new dependent-input decomposition theory;
- broad new noncommuting projection theory;
- harness proves theorem or JSON floats prove theorem;
- peer-reviewed result, arXiv submission, or journal submission.

## Report25 Intent To Evaluate

Report25 recommends:

```text
FORMALIZE_DEFINITIONS_FIRST
```

It proposes that the next object should be a finite formal layer for:

- evaluation charts;
- chart-dependent metric objects;
- chart transport and object identity;
- defect certificates;
- audit-order instability indices;
- exact certificate-library design;
- zero-GPU residual metric audit protocol v0.1.

It proposes notation such as:

```text
c = (P,T,J,R,D,w,A_g,N)
S = {sigma_1, ..., sigma_M}
K_c(sigma) in R^{I_c}
M_c(sigma) = (c, Phi_c(K_c(sigma)))
T_{c->c'} o Phi_c = Phi_{c'} o U_{c->c'}
C = (c, c', K, Delta)
OI_N^op(w) = || D_w restricted to N_add ||_{op,w}
```

It also suggests a candidate next proposition:

```text
OI_N^op(w)=0 iff w is product form
```

Do not accept these definitions uncritically. Check them against the finite
order-defect proof and identify exact proof obligations.

## Your Required Output

Produce a decision memo with the following sections.

### 1. Verdict

Choose exactly one:

- `FORMALIZE_DEFINITIONS_FIRST`
- `BUILD_CERTIFICATE_LIBRARY_FIRST`
- `RUN_LITERATURE_DUPLICATE_AUDIT_FIRST`
- `DESIGN_ZERO_GPU_AUDIT_PROTOCOL_FIRST`
- `STOP_INSUFFICIENT_EVIDENCE`

Explain why in 5-10 sentences.

### 2. What Is Already Safe

List only claims supported by package files. Use file paths.

### 3. What Is Not Safe

List claims that must remain forbidden. Use file paths when possible.

### 4. Definition Audit

Evaluate the proposed definitions for:

- chart;
- roster;
- chart-dependent metric object;
- identity transport;
- defect certificate;
- audit-order instability.

For each one, label it:

- `ACCEPT_AS_IS`
- `ACCEPT_WITH_REVISION`
- `REJECT`
- `NEEDS_PROOF_OR_FILE`

### 5. Next Theorem Candidate

Assess whether the candidate

```text
OI_N^op(w)=0 iff w is product form
```

is already provable from the existing formal note, or whether it needs a new
lemma. Give the exact finite-dimensional proof plan or blocker.

### 6. Certificate Library Priority

Rank these from safest to riskiest:

- product control;
- restricted instability index;
- near-product family;
- judge-order toy;
- contamination-coupling toy;
- ranking-instability toy.

For each, say whether it is `PROVABLE_NOW`, `PLAUSIBLE`, `SPECULATIVE`, or
`BAD_IDEA`.

### 7. Literature / Duplicate-Risk Pressure

Do not perform broad web browsing unless explicitly needed. Based on package
files, say whether duplicate risk remains `MEDIUM`, should be raised, or can be
lowered. Be conservative.

### 8. Requests To Node36 Codex

If you need more data, ask in this exact format:

```text
REQUEST_TO_NODE36_CODEX:
- path or file category:
- why needed:
- claim tested:
- blocking or optional:
```

Use bounded requests only. Do not ask for the entire repository unless a precise
file category cannot answer the question.

### 9. Immediate Task For Codex

Give one concrete implementation/research task for node36, phrased as a
bounded taskbook item. It must not require GPU training.

## Tone

Be strict, mathematical, and anti-hype. The goal is to find the next rigorous
stone, not to defend MaoField.

