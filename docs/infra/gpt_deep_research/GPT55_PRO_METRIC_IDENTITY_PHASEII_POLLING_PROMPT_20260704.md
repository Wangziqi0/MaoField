# GPT-5.5 Pro Zero-Context Prompt -- MaoField Metric-Identity Phase II

You are GPT-5.5 Pro acting as a strict mathematical research collaborator and
skeptical programme reviewer. You must use the attached ZIP package as the
primary project evidence. Do not rely on prior chat memory. Public web or
GitHub may be used only for external literature and publication metadata checks,
not to replace the package or the repository files included in it.

## Task

Start the next MaoField research phase. The previous finite result is already a
bounded preprint-level wedge:

```text
Finite Projection Order Defects in Residual Metric Audits:
A Mathematical Starting Point for Black-Box Evaluation
```

Your task is not to re-sell that result as a broad theory. Your task is to use
it as the first exact certificate for a larger mathematical programme:

```text
When can metric outputs across black-box LLM evaluation charts be treated as
the same measurement object?
```

MaoField's new target is the metric-object identity problem: scores, residuals,
rankings, and capability claims are not automatically model properties. They
are chart-dependent measurement objects. A chart may include prompts, tasks,
judges, rubrics, decoding rules, weights, aggregation rules, model/version
sets, and residualization procedures.

Use this provisional chart notation:

```text
c = (P, T, J, R, D, w, A_g, S)
```

where `P` is prompt/task distribution or prompt protocol, `T` task/domain
structure, `J` judge/evaluator, `R` rubric or score map, `D` decoding/output
protocol, `w` sampling or aggregation weights, `A_g` aggregation rule, and `S`
the model/version/set being evaluated. You may refine this notation if you keep
the refinement finite and auditable.

## Read Order

Read the package in this order:

1. `PACKAGE_README.md`
2. `prompt/GPT55_PRO_METRIC_IDENTITY_PHASEII_POLLING_PROMPT_20260704.md`
3. `from_repo/STATE.md`
4. `from_repo/MD_CATALOG.md`
5. `from_repo/docs/infra/MAOFIELD_METRIC_IDENTITY_PROGRAMME_20260704.md`
6. `from_repo/docs/infra/MAOFIELD_METRIC_IDENTITY_PROGRAMME_ADOPTION_NOTE_20260704.md`
7. all files in `rag_results/`
8. `from_repo/docs/infra/gpt_deep_research/deep_research_metric_field_programme_reframe_report21_20260703.md`
9. `from_repo/docs/infra/gpt_deep_research/deep_research_v21_package_usage_audit_report22_20260703.md`
10. `from_repo/docs/infra/gpt_deep_research/deep_research_formal_preprint_v2_2_strict_review_report23_20260703.md`
11. `from_repo/docs/infra/recovery/V2_5_FINAL_SYNTHESIS_COMPARISON_AND_ADOPTION_20260703.md`
12. `from_repo/docs/infra/recovery/V2_5_FINAL_SYNTHESIS_RELEASE_GATE_NOTE_20260703.md`
13. `from_repo/docs/infra/OPEN_MAOFIELD_FORMAL_ZENODO_PREPRINT_PUBLISHED_20260704.md`
14. `from_repo/docs/infra/debranded_residual_transport/MAOFIELD_PREPRINT_V2_5_FINAL_SYNTHESIS_20260703.tex`
15. `from_repo/docs/infra/debranded_residual_transport/MAOFIELD_PREPRINT_V2_5_FINAL_SYNTHESIS_20260703.pdf`
16. exact witness, harness, bibliography, duplicate-risk, and forbidden-claim files listed in `PACKAGE_README.md`.

## Evidence Boundary

Treat these as facts only insofar as they are supported by package files:

- Open-MaoField public repo exists and carries the finite order-defect surface.
- Formal Zenodo preprint DOI is recorded as `10.5281/zenodo.21190475`.
- Repository/software Zenodo DOI is recorded as `10.5281/zenodo.21157578`.
- The finite theorem concerns finite positive weighted two-way tables.
- The exact rational `2 x 2` witness is certificate-level evidence.
- The deterministic floating-point harness is regression support only.
- Mode B MaoField empirical status remains `insufficient_artifact`.
- Duplicate risk remains `MEDIUM`.

Do not claim peer review, arXiv acceptance, journal acceptance, or broad
mathematical priority. Do not claim that the old MaoField empirical programme
has produced a positive result.

## Forbidden Claims

You must not write or imply:

- MaoField empirical positive result;
- full panel has run;
- checkpoint loading/inference/training/new loss has been completed or authorized;
- MaoField residual, interaction, transport, holonomy, or metric field has been observed in data;
- glass box broken;
- F3 positive or LOSO passed;
- broad new ANOVA theory;
- broad new dependent-input decomposition theory;
- broad new noncommuting-projection theory;
- completed formal system;
- JSON floats prove the theorem;
- the harness proves the theorem;
- philosophical language substitutes for proof.

## Research Questions

Answer the following, strictly separating proven facts, package-supported
claims, conjectures, and proposed future work.

1. What is the cleanest mathematical definition of a chart-dependent metric
   object `M_c(m)` or `M_c(S)`?
2. When are two metric outputs under charts `c` and `c'` legitimately the same
   object rather than merely comparable numbers?
3. What should a transport or invariance condition look like?
4. What is the correct formal version of metric-form fetishism: the process by
   which heterogeneous measurement relations are mistaken for intrinsic model
   properties?
5. How should the finite order-defect theorem be used as a minimal obstruction
   rather than overclaimed as a general theory?
6. Can we define an audit-order instability index, for example
   `OI(w)=||D_w||` or pointwise
   `OI_w(K)=||R_{Q->B}K-R_{B->Q}K||_w`, in a mathematically honest way?
7. What is the smallest next theorem, lemma, counterexample, or exact
   certificate that would advance the programme?
8. What exact certificate library should be built next?
9. What would residual metric audit protocol v0.1 require for LLM evaluation
   without claiming empirical success?
10. Which adjacent mathematical fields create duplicate risk or prior-art risk?

## Required Output Format

Return a structured report with these sections:

1. `Evidence Intake Verdict`
   - Did the package support the stated current status?
   - Which files were decisive?
   - What is missing?

2. `Minimal Safe Programme Statement`
   - 5 to 10 sentences, paper-safe.

3. `Formal Definitions Draft`
   - evaluation chart;
   - chart-dependent metric object;
   - identity/transport/invariance condition;
   - defect certificate;
   - audit-order instability.

4. `Theorem / Counterexample Candidates`
   - list at least three candidates;
   - mark each as PROVABLE_NOW / PLAUSIBLE / SPECULATIVE / BAD_IDEA;
   - specify required assumptions and exact finite objects.

5. `Certificate Library Plan`
   - include at least: the existing `2 x 2` non-product order defect, a product
     control, a near-product perturbation, a judge-order toy, a contamination
     coupling toy, and a ranking instability toy.

6. `Literature and Duplicate-Risk Map`
   - compare against dependent-input ANOVA/Sobol/Shapley, projection theory,
     HELM/BIG-bench, benchmark contamination, LLM-as-judge bias, and metric
     robustness work.
   - keep duplicate risk at MEDIUM unless you can justify a change from
     primary sources.

7. `Residual Metric Audit Protocol v0.1`
   - give a finite, zero-GPU/audit-first protocol;
   - list negative controls and forbidden interpretations.

8. `Requests to Node36 Codex`
   - If the package is insufficient, ask for more data using exactly this form:

```text
REQUEST_TO_NODE36_CODEX:
- path or file category:
- why needed:
- claim tested:
- blocking or optional:
```

9. `Next Decision`
   Choose exactly one:
   - `BUILD_CERTIFICATE_LIBRARY`
   - `FORMALIZE_DEFINITIONS_FIRST`
   - `RUN_LITERATURE_AUDIT_FIRST`
   - `DESIGN_ZERO_GPU_AUDIT_PROTOCOL`
   - `STOP_INSUFFICIENT_EVIDENCE`

## Tone and Discipline

Be ambitious about the mathematical object but conservative about evidence.
The old MaoField empirical work is conceptual ore and a cautionary failure
record, not a positive empirical result. The finite order-defect theorem is a
real exact wedge, not the whole building. Your job is to find the next rigorous
stone.
