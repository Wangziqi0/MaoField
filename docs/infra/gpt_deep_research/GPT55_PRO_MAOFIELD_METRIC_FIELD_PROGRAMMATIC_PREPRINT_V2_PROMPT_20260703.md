# GPT-5.5 Pro Zero-Context Prompt: MaoField Metric-Field Programme / Order-Defect V2 Draft

You are GPT-5.5 Pro acting as a severe mathematical referee, scientific editor,
and programme architect. You are receiving a MaoField package that contains a
finite order-defect theorem, exact witness material, earlier recovery audits,
and a new programmatic reframing note.

Your task is **not** to rubber-stamp a preprint. Your task is to repair and
rewrite the draft into a rigorous:

```text
PROGRAMMATIC_PREPRINT_DRAFT_CANDIDATE_UNDER_LOCK
```

This is not public-release authorization. This is not paper-ready,
preprint-ready, posted, submitted, accepted, or submission-authorized.

## Current Project Boundary

Use the package files as primary context. The current canonical state is:

```text
FORMAL_PREPRINT_DRAFT_GATE_UNDER_LOCK
```

The established mathematical contribution is narrow:

```text
finite positive weighted two-way table
weighted Hilbert space L^2(w)
constant subspace C
centered row-main-effect subspace A
centered column-main-effect subspace B0
additive nuisance space N_add = C + A + B0
product weights iff A is orthogonal to B0
order-independence iff product weights
non-product weights admit an existential pure-main-effect witness
whose true additive residual is zero but whose wrong-order sequential
stripping output is nonzero
exact 2 x 2 rational witness with norm squared 61/177408
```

The larger MaoField direction is programmatic:

```text
MaoField studies whether observed LLM capability, safety, reasoning,
hallucination, alignment, and benchmark metrics are invariant properties of
model behavior or coordinate-dependent artifacts of a weighted evaluation
field.
```

You may use that sentence only as a **studies whether** programme statement,
not as a proven result.

## Why This Pass Exists

A prior draft over-compressed the project into a small `2 x 2` mathematical
note. Report21 argues that the theorem should instead be used as a **wedge**:

```text
finite theorem = minimal obstruction
MaoField = residual metric audit programme
LLM metric black box = target
future audit geometry = research field
```

This is the right strategic direction only if it is written with strict
evidence boundaries.

The core bridge sentence to test and refine is:

```text
A metric residual should not be interpreted as an intrinsic model property
until its invariance under weighting, nuisance-removal order, judge protocol,
and aggregation chart has been audited.
```

This sentence is allowed as a proposed criterion/programme. Do not write it as
a theorem already proved for deployed LLM systems.

## Mandatory Repairs From Node36 Audit

Repair these blockers before polishing:

1. Insert this canonical sentence verbatim:

```text
The floating-point harness is deterministic regression support only; the mathematical claims are carried by the analytic proof and exact rational certificate, not by JSON floats.
```

2. Use or justify a title close to one of these:

```text
Finite Order Defects as Minimal Obstructions to Residual Metric Audits
Order Defects in Weighted Residual Audits: A Finite Obstruction and the MaoField Programme
When Metrics Become Fields: A Finite Order-Defect Obstruction for Black-Box LLM Evaluation Audits
```

If you choose the more ambitious title, compensate with stronger claim
boundaries.

3. Include the full canonical bibliography floor, at minimum:

- Giles Hooker, generalized functional ANOVA diagnostics for dependent
  variables, DOI `10.1198/106186007X237892`.
- Chastaing, Gamboa, Prieur, generalized Hoeffding-Sobol decomposition for
  dependent variables, DOI `10.1214/12-ejs749`.
- Chastaing, Gamboa, Prieur, generalized Sobol sensitivity indices for
  dependent variables, DOI `10.1080/00949655.2014.960415`.
- Owen and Prieur, Shapley value for dependent inputs, DOI
  `10.1137/16M1097717`.
- Iooss and Prieur, Shapley effects for correlated inputs, DOI
  `10.1615/INT.J.UNCERTAINTYQUANTIFICATION.2019028372`.
- Il Idrissi et al., Hoeffding decomposition of functions of random dependent
  variables, DOI `10.1016/j.jmva.2025.105444`.
- Lamboni, ANOVA-type decompositions with non-independent variables, DOI
  `10.1137/24M1712680`; if print metadata is uncertain, cite only DOI /
  publisher online record status.
- Boettcher and Spitkovsky, two projections theory, DOI
  `10.1016/j.laa.2009.11.002`.
- Corach and Maestripieri, products of orthogonal projections and polar
  decompositions, arXiv `1011.5237`.
- Halmos, Two subspaces, DOI `10.1090/S0002-9947-1969-0251519-5`.
- LLM evaluation / metric-complex context: HELM, BIG-bench, LLM-as-judge,
  contamination, hallucination/truthfulness, and trustworthiness evaluations.

4. Retain this exact duplicate-risk status:

```text
MEDIUM duplicate risk
```

Do not claim no close prior work exists.

5. Replace readiness language with one of:

```text
PROGRAMMATIC_PREPRINT_DRAFT_CANDIDATE_UNDER_LOCK
local draft candidate under lock
formal draft candidate under lock
```

Do not say polished, suitable, paper-ready, preprint-ready, public-postable, or
submission-authorized.

## Required Manuscript Shape

Produce a standalone LaTeX draft with this logic:

1. **Abstract.** Three layers: finite theorem, exact witness, MaoField
   residual metric audit programme. Must say no empirical MaoField result is
   reported.
2. **Introduction: the residual problem in black-box evaluation.** Explain that
   modern LLM evaluation is a coupled measurement complex, not a transparent
   scalar score.
3. **Finite obstruction theorem.** Give the exact finite weighted setup,
   product-weight iff orthogonality theorem, order-independence equivalence,
   and existential pure-main-effect artifact statement.
4. **Exact rational certificate.** Include the `2 x 2` witness exactly.
5. **What the theorem does and does not imply.** Make the finite obstruction
   boundary explicit.
6. **MaoField programme: metric complexes as audit objects.** Define future
   audit objects such as evaluation table, metric field, residual chart,
   order-defect audit, contamination coupling, judge coupling, aggregation
   coupling, temporal drift.
7. **Future mathematical objects.** Include concrete objects only:
   multi-axis finite weighted audit cubes, near-product perturbation bounds,
   commutator norm as instability index, residual chart equivalence classes,
   weight-sensitive leaderboard rank stability, judge-order defect, and
   benchmark-contamination coupling certificates.
8. **Related work and duplicate risk.** Include the full bibliography floor and
   explicitly state what this note does not claim.
9. **Limitations and reproducibility.** Keep the harness boundary and Mode B
   boundary explicit.

## Exact Witness To Preserve

Use this exact certificate:

```text
w = (1/11) [[1,2],[3,5]]
w_Q = (3/11, 8/11)
w_B = (4/11, 7/11)
K = (7/11, -4/11, 7/11, -4/11) in B0
(I - P_N_add)K = 0
R_{B->Q}K = 0
R_{Q->B}K = (1/32, 5/168, -1/96, -1/84)
||R_{Q->B}K||_w^2 = 61/177408
```

## Hard Forbidden Claims

Do not claim any of the following:

- paper-ready, preprint-ready, public-postable, posted, submitted, accepted, or
  submission-authorized;
- broad new ANOVA theory;
- broad new dependent-input decomposition theory;
- broad new noncommuting-projection theory;
- completed formal system;
- MaoField empirical positive result;
- full panel has run;
- checkpoint inference;
- training;
- new loss;
- observed MaoField residual, interaction, transport, or holonomy field;
- glass box broken;
- F3 positive;
- LOSO passed;
- JSON floats prove theorem;
- harness proves theorem.

Mode B MaoField empirical status remains:

```text
insufficient_artifact
```

## Output Format

Return:

1. A corrected standalone LaTeX manuscript.
2. A short change log listing every repair made against the previous package.
3. A source-map table mapping major claims to package files.
4. A forbidden-claim scan table.
5. A final gate verdict choosing exactly one:

```text
PROGRAMMATIC_PREPRINT_DRAFT_CANDIDATE_UNDER_LOCK
REQUEST_BIBLIOGRAPHY_PATCH_BEFORE_DRAFT
REQUEST_PROOF_WORDING_PATCH_BEFORE_DRAFT
STOP_OR_PARK_DRAFT_GATE
```

Before finalizing, explicitly check theorem/proof consistency, exact witness
arithmetic, the verbatim harness-boundary sentence, full bibliography floor,
`MEDIUM duplicate risk`, no forbidden claims, and MaoField as programmatic
future work only.
