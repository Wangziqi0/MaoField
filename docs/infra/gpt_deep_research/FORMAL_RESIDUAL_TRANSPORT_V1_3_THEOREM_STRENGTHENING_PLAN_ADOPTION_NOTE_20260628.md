# Formal Residual Transport v1.3 Theorem-Strengthening Plan Adoption Note

Date: 2026-06-28 CST
Scope: node36 adoption of report (31), archived as
`deep_research_formal_residual_transport_v1_3_theorem_strengthening_plan_20260628.md`.

## Adopted Classification

```text
formal_v1_3_weighted_anova_order_defect_plan_accepted_with_guards
```

Node36 adopts report (31) as a narrow Mode A mathematical planning verdict:
Formal v1.3 should first pursue the product versus non-product weight boundary,
formalized as a finite weighted ANOVA order-defect theorem/no-go package.

The accepted target is:

```text
weighted_anova_order_defect_product_weight_boundary
```

## What This Accepts

This adoption accepts a next-step theorem target, not a completed theorem stack.
The report recommends turning the v1.2 product-weight Hoeffding boundary into
precise finite-dimensional statements:

1. Product-weight iff zero-mean q-only and b-only main-effect subspaces are
   orthogonal under the observed finite positive weight.
2. Sequential main-effect stripping is order-independent iff the weight is
   product-form.
3. In the non-product case, sequential stripping can create a fake residual
   from a pure main effect even when the true orthogonal additive residual is
   zero.
4. Minimal zero-GPU harness checks should verify theorem identities and the
   non-product no-go witness only.

These are adopted as v1.3 proof obligations and implementation targets. They
are not yet adopted as proved, implemented, or empirically observed facts.
Report (31)'s internal classification
`v1_3_theorem_strengthening_plan_accepted` is therefore treated as a Pro-report
classification, not as a node36 completed-theorem verdict.

## Evidence Boundary

Report (31) is a claim-source and mathematical review over the uploaded bundle.
It does not add MaoField data, load checkpoints, run inference, run a full
panel, or authorize training/new loss work.

Strongest allowed local status remains:

```text
definitions_and_harness_viable_only
```

Mode B MaoField empirical status remains:

```text
insufficient_artifact
```

## Still Forbidden

This adoption does not authorize any of the following claims:

- completed formal system;
- theorem stack complete;
- Formal v1.3 implemented or proved;
- empirical MaoField upgrade;
- full panel has run;
- 16-cell full-panel aggregate exists;
- checkpoint loading, inference, training, or new loss is authorized;
- observed MaoField residual, interaction, quotient-residual, transport, or
  holonomy field;
- glass box broken;
- LOSO passed;
- F3 positive.

## Next Allowed Step

The next useful GPT-5.5 Pro task is a proof-level audit of the proposed
v1.3 order-defect theorem package:

- check the finite-dimensional definitions;
- verify or repair the product-weight iff proof;
- verify or repair the sequential-order no-go proof;
- specify exact minimal synthetic harness checks;
- reject or refine the target if the theorem statement is false or underspecified.

No full-panel, checkpoint, training, or MaoField empirical work is implied.
