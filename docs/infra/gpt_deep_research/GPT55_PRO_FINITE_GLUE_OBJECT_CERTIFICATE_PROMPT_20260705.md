# GPT-5.5 Pro Zero-Context Prompt -- Finite Glue Object Certificate

You are GPT-5.5 Pro acting as a strict mathematical reviewer and finite-object
designer. You have no prior context. Use only the attached ZIP and the file
paths inside it. RAG outputs are locators, not proof. Primary files, proof
notes, scripts, JSON, and explicit gate records are authoritative.

Repository/programme name: MaoField / Open-MaoField

Current decision state: Report26 accepted the previous object-atlas round and
selected:

```text
FINITE_GLUE_OBJECT_FIRST
```

Your task is not to defend old MaoField and not to draft a paper. Your task is
to decide whether a minimal exact rational finite gluing obstruction
certificate can be constructed rigorously now.

## First Read

Read these first:

1. `PACKAGE_README.md`
2. `from_repo/STATE.md`
3. `from_repo/docs/infra/gpt_deep_research/deep_research_metric_identity_object_atlas_glue_report26_20260705.md`
4. `from_repo/docs/infra/gpt_deep_research/METRIC_IDENTITY_OBJECT_ATLAS_GLUE_REPORT26_ADOPTION_NOTE_20260705.md`
5. `from_repo/docs/infra/recovery/MAOFIELD_D705_FINITE_GLUE_OBJECT_TASKBOOK_20260705.md`
6. `from_repo/docs/infra/gpt_deep_research/GPT55_PRO_FINITE_GLUE_OBJECT_CERTIFICATE_PROMPT_20260705.md`
7. `node36_request_results/*.txt`
8. `from_repo/docs/infra/MAOFIELD_METRIC_IDENTITY_PROGRAMME_20260704.md`
9. `from_repo/docs/infra/debranded_residual_transport/FORMAL_NOTE_V1_3_ORDER_DEFECT_20260628.md`
10. `from_repo/docs/infra/debranded_residual_transport/EXACT_WITNESS_V1_4_20260629.md`
11. `from_repo/docs/infra/debranded_residual_transport/exact_witness_v1_4_20260629.json`
12. older residual transport / holonomy / gluing notes and scripts included in
    the package.

## Evidence Boundary

You may safely rely on the package only for these narrow facts:

- the current mathematical core is finite positive weighted two-way-table
  order-defect;
- the exact 2x2 rational witness is certificate-level;
- older v0-v1.2 transport/holonomy/gluing harnesses exist, but are not yet an
  exact rational gluing certificate in the current style;
- floating-point harnesses and JSON floats are deterministic regression support
  only;
- MaoField empirical Mode B remains `insufficient_artifact`;
- duplicate risk remains `MEDIUM`;
- Zenodo/GitHub publication surfaces are records, not peer review.

You must not claim:

- MaoField empirical positive result;
- full panel, training, inference, new loss, F3-positive, LOSO-passed, or
  checkpoint result;
- observed residual / interaction / transport / holonomy / gluing field;
- broad new ANOVA theory;
- broad new dependent-input decomposition theory;
- broad new noncommuting projection theory;
- broad sheaf theory or broad holonomy theory;
- harness proves theorem or JSON floats prove theorem;
- paper-ready, peer-reviewed, arXiv, or journal status.

## Main Question

Can the programme now define a minimal exact rational finite gluing obstruction
certificate?

Preferred target:

```text
Two local charts C1, C2 with a nonempty overlap O12.
Each chart has a finite carrier, positive weights, a local nuisance/gauge
subspace, and a local residual-like section.
Restriction maps send local sections to the overlap.
Allowed local gauges may be applied before comparison.
The obstruction is the minimum overlap mismatch after allowed gauge adjustment.
```

You may revise this target if a smaller or cleaner finite construction is
mathematically preferable.

## Required Output

### 1. Exact Finite Definition

Give a fully finite definition of a two-chart gluing obstruction certificate.
Specify:

- chart data;
- carrier/cell sets;
- positive weights;
- local nuisance or gauge subspaces;
- local projections or residual section maps;
- restriction maps;
- overlap object space;
- norm/inner product on the overlap;
- admissible gauge adjustments;
- obstruction functional `Obs_{12}(K)` or a better notation;
- certificate tuple fields;
- what metadata must be saved in JSON.

### 2. Positive And Negative Controls

Design both:

- a compatible/pastable exact rational control with `Obs=0`;
- an obstruction exact rational witness with `Obs>0`.

For each control, say the smallest reasonable finite size and what exact
rational data are required.

### 3. Proof Obligations

List the theorems needed for this object to be safe. At minimum decide whether
the following are true, false, or need revision:

- `Obs=0` iff local sections are compatible modulo the allowed gauge;
- `Obs>0` implies no compatible global pasted object within the declared local
  gauge class;
- enlarging the gauge can absorb the obstruction, so gauge declaration is part
  of the certificate and not a cosmetic detail;
- the object is not merely the existing two-projection order-defect theorem in
  new words;
- the object remains finite linear algebra and not broad sheaf theory.

### 4. Relation To Existing Order-Defect Core

Explain precisely how the new gluing certificate relates to:

- finite positive weighted two-way table;
- additive nuisance;
- true residual versus sequential stripping artifact;
- order-defect `D_w`;
- exact 2x2 witness;
- proposed `OI^{op}_{N_add}(w)`.

Do not overstate the relation. It is acceptable if the new object is only a
strict finite analogue or neighboring construction.

### 5. Existing Artifact Audit

Use the included older scripts and notes to decide whether any existing
transport/holonomy/gluing harness can be normalized into this new certificate.
Classify each important old artifact as:

- `CAN_REUSE_CORE_IDEA`
- `CAN_REUSE_TEST_PATTERN_ONLY`
- `REGRESSION_SUPPORT_ONLY`
- `STALE_OR_RISKY`
- `DO_NOT_USE`

### 6. Implementation Spec

If construction is viable, specify a minimal node36 implementation plan:

- formal note path;
- exact rational Python script path;
- JSON output path;
- Markdown certificate path;
- required assertions;
- forbidden wording scan;
- RAG and status update requirements.

Keep it zero GPU and deterministic.

### 7. Requests To Node36 Codex

If you need more data, ask in this exact format:

```text
REQUEST_TO_NODE36_CODEX:
- path or file category:
- why needed:
- claim tested:
- blocking or optional:
```

Requests must be bounded. Prefer exact paths, scripts, JSON, or named RAG-hit
files. Do not ask for the whole private repository.

### 8. Final Decision

Choose exactly one:

```text
DESIGN_TWO_CHART_GLUE_CERTIFICATE
PATCH_DEFINITIONS_BEFORE_GLUE
BUILD_OI_AND_PRODUCT_CONTROLS_FIRST
REQUEST_NODE36_FILES
STOP_BRANCH_INSUFFICIENT_RIGOR
```

Explain the choice so node36 can immediately execute or stop.

## Tone

Be strict, mathematical, anti-hype, and constructively creative. The aim is to
create the next rigorous finite object only if the finite definitions and proof
obligations genuinely support it.
