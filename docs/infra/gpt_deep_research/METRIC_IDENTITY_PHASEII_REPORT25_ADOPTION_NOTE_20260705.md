# Metric-Identity Phase II Report25 Adoption Note

Date: 2026-07-05 CST

Source report:

```text
docs/infra/gpt_deep_research/deep_research_metric_identity_phaseii_report25_20260705.md
```

Source attachment sha256:

```text
42a4590ab385d18a3d3f5f3997162e44a2f7a7f1d4380f9d8de9c9b4889c06b1
```

## Verdict

Adopt as a **Phase-II direction-setting audit**, not as proof, empirical
evidence, or authorization to expand MaoField claims.

Operational verdict:

```text
REPORT25_ACCEPTED_AS_DECISION_INPUT_FORMALIZE_DEFINITIONS_FIRST
```

## What Report25 Means

Report25 supports the current narrowed state: MaoField is safe only as a
finite, audit-facing mathematical programme around metric-object identity. It
does not support broad MaoField theory, observed LLM residual fields, empirical
positive results, or an upgraded Mode B line.

Its central recommendation is:

```text
FORMALIZE_DEFINITIONS_FIRST
```

The intended next object is not another glass-box claim. It is a finite
definition layer for:

- evaluation charts;
- chart-dependent metric objects;
- chart transport and object identity;
- defect certificates;
- audit-order instability indices;
- exact certificate-library design;
- a zero-GPU residual metric audit protocol.

## Accepted Technical Directions

The following are accepted as next-step research targets, subject to proof
checks and local evidence gates:

1. Separate the chart from the evaluated roster:

   ```text
   c = (P,T,J,R,D,w,A_g,N)
   S = {sigma_1, ..., sigma_M}
   ```

2. Define finite chart outputs as processed objects:

   ```text
   K_c(sigma) in R^{I_c}
   M_c(sigma) = (c, Phi_c(K_c(sigma)))
   ```

3. Define identity only through declared transport and an intertwining
   condition:

   ```text
   T_{c->c'} o Phi_c = Phi_{c'} o U_{c->c'}
   ```

4. Define defect certificates as finite witness tuples:

   ```text
   C = (c, c', K, Delta)
   Delta = T_{c->c'} M_c(K) - M_{c'}(U_{c->c'}K)
   ```

5. Promote the existing order-defect theorem into a narrow instability-index
   object only after proof review:

   ```text
   OI_N^op(w) = || D_w |_{N_add} ||_{op,w}
   Candidate: OI_N^op(w)=0 iff w is product form.
   ```

6. Build an exact certificate library in this order:

   ```text
   product control
   restricted instability index
   near-product family
   judge-order toy
   contamination-coupling toy
   ranking-instability toy
   ```

7. Design zero-GPU residual metric audit protocol v0.1 with negative controls
   first.

## Boundaries Preserved

These boundaries remain active:

- Mode B MaoField empirical status remains `insufficient_artifact`.
- Duplicate risk remains `MEDIUM`.
- The finite order-defect theorem remains about finite positive weighted
  two-way tables.
- The exact 2x2 rational witness remains the certificate-level example.
- Floating-point harnesses and JSON floats remain regression support only.
- No full panel, training, inference, new loss, F3-positive, LOSO-passed, or
  observed residual/interaction/transport/holonomy claim is authorized.
- No broad new ANOVA, dependent-input decomposition, or noncommuting projection
  theory claim is authorized.

## Requests Carried Forward

Report25 asked node36 for two optional evidence searches before expanding the
certificate library:

1. Find any existing finite 2x3 / 3x3 exact-certificate notes, scripts, scratch
   markdowns, or JSON artifacts under `docs/infra/` or `scripts/`.
2. Find any local proof note, scratch derivation, or audit memo on how
   `P_A`, `P_B0`, `P_N`, or `D_w` depend on the positive weight table `w`.

These are optional for the immediate decision package, but important before
claiming near-product perturbation bounds.

## Next Pro Use

The next Pro round should be asked to decide the immediate research branch
rather than draft a paper body. The recommended default is:

```text
FORMALIZE_DEFINITIONS_FIRST
```

If Pro needs more material, it should request it from node36 using a bounded
request format that names the file category, why it is needed, the claim being
tested, and whether the request is blocking.

